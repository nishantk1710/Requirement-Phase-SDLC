pydantic>=2.7,<3
pydantic-settings>=2.2
SQLAlchemy>=2.0.25
aiosqlite>=0.19
PyYAML>=6.0
# LLM SDKs (imported lazily inside providers; not needed for mock/dev)
anthropic>=0.40          # provider=foundry (AnthropicFoundry on Azure AI Foundry)
openai>=1.30             # provider=azure (alternative)
# Optional document loaders — imported lazily; only needed to ingest .docx/.pdf sources
# (the synthetic corpus is all text/csv/json, so the suite runs without these).
python-docx>=1.1         # loaders._load_docx  (.docx)
pdfplumber>=0.11         # loaders._load_pdf   (.pdf)
# P6 review API (HITL) — served locally; frontend is a minimal Vite/React app
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9   # FastAPI file uploads (document intake from the UI)
# P8 orchestration — LangGraph StateGraph with interrupt_before + resumable checkpointer
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=2.0
# dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27              # FastAPI ASGI test client (async)
