"""Generation: renders approved requirements into the IEEE-830 (Wiegers) SRS template
and the RTM. The template structure + mappings live in `srs_template.py`; the actual
generator (G1) is built in Phase P7."""
